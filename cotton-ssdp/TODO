make mio/tokio features
make advertise/subscribe features
Cow<'static> for input strings?

Hasher instead of thread_rng()
 - hash over network interfaces sb unique

vary phase 1,2,3 timings but keep phase 0 timings on round numbers
 - needs *absolute* wall time :(

monotonic time instead of Instant::now()
 - needs MonotonicTime *object*, now() is a method, what's its lifetime?

no_std https://github.com/rust-lang/rust/pull/104265

ipv6
